# Code

## Diff

For each condition the diff adds that input must now satisfy — a required field, a new validation, a renamed key, a narrower type — find the data files and the callers already in the repository that the condition applies to. Report each one that does not satisfy it.
