# Decisions

One page each. The entry test and the limit are in `CLAUDE.md`. Numbers are not copied here; they live in `evals/BASELINE.md`. A decision that no longer governs anything that exists moves to `archive/` and leaves this table.

| # | Decision |
|---|---|
| 0001 | The review skill is a process shell, not review knowledge |
| 0002 | A review ends when its candidate lists are walked |
| 0003 | What counts as a finding |
| 0004 | One reference model decides; other models are observed |
| 0006 | Build: the verdict comes from a script; test-first, refactoring and a third check author are not instructed |
