`request.md` is what the owner asked for. Write the contract for this unit of work as `brief.md`: the goal, and a table of acceptance criteria with the columns `AC`, `Behaviour`, `Check`, where `Check` is one shell command, run from the repo root, that exits 0 when the criterion holds. Write the check files under `checks/`. Do not implement the feature. The owner will read `brief.md` and sign it; a builder who has not seen this conversation will build from it.
