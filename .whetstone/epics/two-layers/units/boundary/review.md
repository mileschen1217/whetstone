---
subject: git diff boundary-accepted..HEAD (two-layers unit boundary)
independent: true
---
- skills/line.md:7 — an accepted `epic.md` that has no `Outside:` line (every epic accepted before 0.1.4: this repo's `two-layers` epic, the `ship-memory` fixture's `low-stock` epic) is neither "the Outside: line of the accepted epic.md" nor "neither page", so the sentence names no source for it and the reader picks one without an error; evidence/AC-4.log shows all six `ship-memory` pages wrote `Boundary: none: a person or a thing outside the repo`, the value skills/ship/SKILL.md:24 defines as "no page", while that fixture has an accepted epic page; the ship graders and ac4.sh read no `Boundary:` line and cannot catch it.
- skills/line.md:7 — "the system page under .whetstone/memory/" is no longer identified: 0.1.3 named it by its content (inside, outside, table of exchanges), HEAD drops that, no skill writes such a page (BACKLOG.md:41 defers it to a later epic) and no other file defines it; with a memory page that is not one (`.whetstone/memory/plugin-rules.md` here, `outbound.md` in `ship-memory`) whether the first source applies is the reader's own call, and the page then says `Boundary: system page` with nothing to check it against.
- skills/line.md:15 — two clauses are outside the brief: "an open finding … a finding whose fix is already in the range is `logged`" and "a format, a reply or a protocol between programs, even across the boundary, fixes no cell either"; brief.md:23 fixes test 2 as unchanged apart from the fixed sentences, brief.md:68 puts U3 out of scope, and no `disputed.md` or `decisions.md` line records either clause (skills/build/SKILL.md:15-16). The `logged` clause moves a finding below the line on the builder's word that it is fixed, which REQ-9 (epic.md:31) reserves for a reader who did not write the fix and admits only after measurement; it also assigns a tag from test 2 while skills/ship/SKILL.md:37 takes the Follow-up tag from test 1 only, and by ship:20 the row stops blocking. The program-to-program clause and the fourth cell ("what happens when it is not there or fails") give opposite answers for a program's reply when a thing is absent, which is the row disputed.md names (`score` prints null); two readers of the same page now count differently.
- skills/ship/SKILL.md:24 — the `Boundary:` fact line changes the pr.md page format, which brief.md:62 fixes as unchanged and the brief's Interface (lines 13-25) does not list ship/SKILL.md among the files changed; the brief demands it elsewhere ("the page says which it took", line 18), so this is the brief contradicting itself, and skills/build/SKILL.md:15 says that goes in `disputed.md`, which has no such line.
- evals/BASELINE.md:392 — the unit `boundary` table holds the placeholders "(evidence AC-3)" to "(evidence AC-6)" and empty count and USD cells, while AC-8's Behaviour requires this unit's two real-page counts and its cost; the numbers exist in the evidence (AC-3.log: 5 and 4, 0.66 + 0.67 USD; AC-4.log: 18/18, 3.59 USD; AC-5.log: 3/3, 2/2 with costs) and line 386's "Numbers below are filled in by the unit's evidence files" names no mechanism that fills them. ac8.sh:5-6 greps only `10`, `13`, `18/18` and `USD`, all supplied by the unit `line` rows and the table header, so verdict.md says AC-8 PASS.
