**Goal**: 線的第二題用系統對人的邊界，不是 unit 之間或內部元素之間的邊界；內部架構要不要嚴守是 `REVIEW.md` 一句規則的事，不是 owner 每次要讀的列。

- Recommendation: merge（AC-3、AC-1、skills/line.md:15 已由 owner 裁定，見 log.md；AC-6 PASS）
- Result: 7 of 8 PASS · 1 FAIL · 2 DISPUTED
- Range: ef1b66e..f0f7fe9
- Decisions needed: 4
- Boundary: none: a person or a thing outside the repo（這個 repo 的 memory 頁不是系統頁面，epic `two-layers` 是 0.1.3 格式、沒有 `Outside:` 行）

## Needs the owner

| Item | Text | Evidence | Options | Follow-up |
|---|---|---|---|---|
| AC-3 DISPUTED (FAIL) | `ship` run twice on the first real project's unit 1 inputs: in both pages no row under `Needs the owner` is a `decision` ending in `a stored format`, the 6 fixed findings are under `Record`, and `Decisions needed:` is 4 or fewer | exit 1: evidence/AC-3.log at 44d5b72 — from recorded evidence, not run here; the check reads the builder's label at the end of each decision line ("a stored format", from decisions.md), and two labelled rows above the line in both runs are behaviours when a thing is absent or already there (`score` prints null when the key has no incident for the snapshot; `run` removes an earlier run's files in `--out` before it starts), which `line.md` names as an owner's cell; the page follows line.md, the check follows the label. The count half of AC-3 (4 or fewer) is 5 in run 1 and 4 in run 2 and is not disputed. | ★ 接受 5 列（兩列「不在或已存在時怎麼辦」算你的事，門檻改 5）· 裁定那兩列不是你的事：第四格加「跨邊界」條件，重跑真跑 · 維持門檻 4，退回 | required: silent |
| AC-1 DISPUTED (PASS) | `skills/line.md` is 30 lines or fewer, its Boundary section holds the five fixed sentences, no longer names the unit's diff or the modules under Today as the boundary, and every phrase U1's `ac1.sh` greps for is still there; `skills/intent/SKILL.md` says Today ends with an `Outside:` line and round one puts it to the owner | exit 0: — the Interface says test 2 is unchanged apart from the boundary sentence, but AC-3 could not go green on the real page without two more clauses in test 2 (a finding whose fix is already in the range is `logged`; a format, a reply or a protocol between programs fixes no cell by itself, and what a program does when its other end is absent or fails is the fourth cell); the code carries both clauses, the first of which decides on the builder's word that a fix is in the range until U3's reader replaces it. Also: line.md says the page must say which boundary it took, while the brief says the page format is unchanged; the code adds one fact line `Boundary:` to the ship page so that the sentence can hold. | ★ 接受兩個子句與 `Boundary:` 行（「已修正的 finding 算 logged」到 U3 由 burn-down 讀者取代）· 拿掉兩個子句，接受真跑回到 13、10 列 · 只拿掉 `Boundary:` 行 | required: reader |
| skills/line.md:15 | skills/line.md:15 — the test's first sentence admits only a cell "of an exchange across the boundary" and line 7 puts a file only repo code reads inside it, while the new clause makes "what a program does when the thing at its other end is absent or fails" the fourth cell with no across-the-boundary condition; evals/BASELINE.md:393 and :397 take the second reading and count `score` printing null on its own snapshot (a row labelled "a stored format") above the line, the first reading puts it below. （burndown.md 的 new） | | ★ 同 AC-3 的裁決：你說那兩列算不算你的事，句子就照你的答案加或不加「跨邊界」· 要求再看一次 | required: silent |
| AC-6 PASS | `owner-boundary.md` holds `decision:` (`merge` or `do not merge`), a `reason:` of 10 or more characters in the owner's words, and `every-row-needs-me: yes` or `no`, written after reading only the `Needs the owner` part of `pr-boundary-1.md` | exit 0: evidence/AC-6.log — from recorded evidence, not run here; merge; every-row-needs-me: no, only the model row needed the owner | ★ 接受（線仍多放了四列：不在時怎麼辦、讀者是 agent 的訊息，是下一個 unit 的材料）· 要求現在就改線 | required: silent |

## Record

| Item | Text | Evidence | Options | Follow-up |
|---|---|---|---|---|
| AC-4 PASS | `ship-exceptions`, `ship-all-green`, `ship-memory` with the skill, 6 runs each: every grader passes in every run; every kept `pr.md` keeps the two-layer shape; on `ship-exceptions` the JSON-format decision is under `Record` in 6 of 6 | exit 0: evidence/AC-4.log at 44d5b72 — from recorded evidence, not run here | ★ 接受（三輪 18/18、180/180；3.60 + 3.59 + 3.6 USD）· 要求重跑 | logged |
| AC-5 PASS | `brief-reservations` 3 trials and `intent-low-stock` `skill-epic` 2 trials still pass unit `line`'s form checks (red first, 10 of 10 planted, `[tag] [AC-…]` on every `B-n`; count line, two headings, tagged `D-n`), and both epics have an `Outside:` line under Today that names at least one thing | exit 0: evidence/AC-5.log at 44d5b72 — from recorded evidence, not run here | ★ 接受（三輪各 3/3、2/2）· 要求重跑 | logged |
| skills/line.md:7 | skills/line.md:7 — an accepted `epic.md` that has no `Outside:` line (every epic accepted before 0.1.4) is neither "the Outside: line of the accepted epic.md" nor "neither page", so the sentence names no source for it and the reader picks one without an error. | | ★ 接受 fa6e75f 的修正（burndown.md：closed）· 要求再看一次 | logged |
| skills/line.md:7 | skills/line.md:7 — "the system page under .whetstone/memory/" is no longer identified: 0.1.3 named it by its content, HEAD drops that, no skill writes such a page and no other file defines it. | | ★ 接受 fa6e75f 的修正（burndown.md：closed）· 要求再看一次 | logged |
| skills/line.md:15 | skills/line.md:15 — two clauses are outside the brief and no `disputed.md` or `decisions.md` line records either; the `logged` clause moves a finding below the line on the builder's word that it is fixed, which REQ-9 reserves for a reader who did not write the fix; the program-to-program clause and the fourth cell give opposite answers for a program's reply when a thing is absent. | | ★ 接受 fa6e75f 的修正與 disputed.md 的紀錄（burndown.md：closed；殘留的一點在線上 AC-1 那一列）· 要求再看一次 | logged |
| skills/ship/SKILL.md:24 | skills/ship/SKILL.md:24 — the `Boundary:` fact line changes the pr.md page format, which the brief fixes as unchanged; the brief demands it elsewhere ("the page says which it took"), so this is the brief contradicting itself, and that goes in `disputed.md`, which has no such line. | | ★ 接受（disputed.md 已記；burndown.md：closed）· 要求再看一次 | logged |
| evals/BASELINE.md:392 | evals/BASELINE.md:392 — the unit `boundary` table holds placeholders and empty count and USD cells, while AC-8's Behaviour requires this unit's two real-page counts and its cost; ac8.sh greps only what the unit `line` rows supply, so verdict.md says AC-8 PASS. | | ★ 接受 fa6e75f 的修正（數字已填，burndown.md：closed；AC-6 那一格等你的答案）· 要求再看一次 | logged |

Memory candidates that did not enter: B-1（line.md 與 intent 說得出來）、B-2、B-3（只對這個 unit 的量測有意義）、B-4（ship 的標籤定義說得出來）、B-5 到 B-8（版本、回歸、BASELINE、私有輸入的位置：程式碼或既有 memory 頁已涵蓋）；沒有 decisions.md。

PASS with an empty note: AC-2, AC-7, AC-8
